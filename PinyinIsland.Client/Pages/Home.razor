@page "/"
@inject NavigationManager Nav
@inject IDialogService DialogService
@inject ISnackbar Snackbar
@inject IJSRuntime JS
@inject IProgressService ProgressService
@inject HttpClient Http

<PageTitle>เกาะมหาสมบัติพินอิน - Pinyin Island</PageTitle>

<div class="landing-container">
    <!-- Top Quick Controls (Kid & Parent/Teacher Actions) -->
    <header class="top-controls">
        <div class="controls-left">
            <div class="grade-badge">
                <span class="badge-emoji">🎒</span>
                <span class="badge-text">ป.1 เรียนสนุก</span>
            </div>
        </div>

        <div class="controls-right">
            <!-- BGM Audio Toggle Button (Tactile 3D Round) -->
            <button type="button"
                    class="tactile-icon-btn btn-blue @(_isMuted ? "muted" : "")"
                    @onclick="ToggleSound"
                    title="@(_isMuted ? "เปิดเสียงเพลง" : "ปิดเสียงเพลง")"
                    aria-label="สลับเสียงเพลง">
                <MudIcon Icon="@(_isMuted ? Icons.Material.Filled.VolumeOff : Icons.Material.Filled.MusicNote)"
                         Class="tactile-btn-icon" />
            </button>

            <!-- Reset Progress Button (Parents / Teachers gentle protection) -->
            <button type="button"
                    class="tactile-icon-btn btn-red"
                    @onclick="OpenResetConfirmDialog"
                    title="เริ่มเล่นใหม่ทั้งหมด (สำหรับครู/ผู้ปกครอง)"
                    aria-label="เริ่มเล่นใหม่">
                <MudIcon Icon="@Icons.Material.Filled.RestartAlt"
                         Class="tactile-btn-icon" />
            </button>
        </div>
    </header>

    <!-- Main Hero Centerpiece -->
    <main class="hero-content">
        <!-- Pinyin Island Bubble Logo Header -->
        <div class="logo-wrapper floating-anim">
            <img src="assets/images/logo-pinyin-island.jpeg"
                 alt="Pinyin Island เกาะมหาสมบัติพินอิน"
                 class="pinyin-logo" />
        </div>

        <!-- Animated Mascot Showcase (Panda Pirate) -->
        <div class="mascot-stage" @onclick="ToggleSound" title="แตะน้องหมีเพื่อเปิด/ปิดเสียง" style="cursor: pointer;">
            <div class="mascot-frame bounce-anim">
                <video id="mascotVideo"
                       class="mascot-video"
                       autoplay
                       loop
                       playsinline
                       poster="assets/images/panda-pirate.jpeg">
                    <source src="assets/videos/panda-waving.mp4" type="video/mp4" />
                    <!-- Fallback image -->
                    <img src="assets/images/panda-pirate.jpeg" alt="น้องแพนด้าโจรสลัด" class="mascot-fallback" />
                </video>
                <div class="mascot-shadow"></div>
            </div>

            <div class="island-speech-bubble">
                <span>พร้อมไปล่าสมบัติกับน้องหมีหรือยัง? ✨</span>
            </div>
        </div>

        <!-- Primary Action Button: Giant 3D Pulsing "เริ่มเล่นเลย!" -->
        <div class="cta-container">
            <button type="button"
                    class="play-now-tactile-btn"
                    @onclick="StartGame"
                    aria-label="เริ่มเล่นเลย">
                <div class="play-btn-content">
                    <div class="play-icon-circle">
                        <MudIcon Icon="@Icons.Material.Filled.PlayArrow" Class="play-arrow-svg" />
                    </div>
                    <div class="play-btn-labels">
                        <span class="play-btn-primary">เริ่มเล่นเลย!</span>
                        <span class="play-btn-sub">START ADVENTURE</span>
                    </div>
                </div>
            </button>
        </div>
    </main>

    <!-- Floating Treasure Chest in the Right Sea Area -->
    <div class="chest-floating-sea" title="หีบสมบัติพินอิน">
        <div class="chest-inner-anim">
            <img src="assets/images/chest-treasure.jpeg" alt="หีบสมบัติพินอินเรืองแสง" class="chest-sea-img" />
            <div class="chest-sparkle">✨</div>
        </div>
    </div>

    <!-- Footer Wave / Soft Credit -->
    <footer class="landing-footer">
        <span>🏝️ ผจญภัยออกเสียงพินอิน สนุก ปลอดภัย สำหรับเด็กวัย 6-7 ขวบ</span>
        <span class="version-badge" title="เวอร์ชันแอปพลิเคชัน">@_appVersion</span>
    </footer>
</div>

<!-- Parent / Teacher Reset Confirmation Dialog -->
<MudDialog @bind-Visible="_resetDialogVisible" Options="_dialogOptions">
    <TitleContent>
        <div class="dialog-title-custom">
            <span class="dialog-title-icon">🔒</span>
            <span>ยืนยันเริ่มเล่นใหม่ (สำหรับผู้ปกครองหรือคุณครู)</span>
        </div>
    </TitleContent>
    <DialogContent>
        <div class="dialog-body-custom">
            <p class="dialog-warning-text">
                คุณต้องการรีเซ็ตดาว คะแนน และความก้าวหน้าการเรียนรู้พินอินทั้งหมดเพื่อเริ่มใหม่ใช่หรือไม่?
            </p>
            <div class="parent-math-check">
                <p class="math-question">ตอบคำถามเพื่อยืนยัน: <strong>3 + 4 = ?</strong></p>
                <MudTextField @bind-Value="_mathAnswer"
                              Variant="Variant.Outlined"
                              Placeholder="ใส่คำตอบตัวเลข"
                              InputType="InputType.Number"
                              Class="mt-2"
                              Immediate="true" />
            </div>
        </div>
    </DialogContent>
    <DialogActions>
        <MudButton Variant="Variant.Text"
                   Color="Color.Default"
                   Size="Size.Medium"
                   OnClick="CloseResetDialog">
            ยกเลิก
        </MudButton>
        <MudButton Variant="Variant.Filled"
                   Color="Color.Error"
                   Size="Size.Medium"
                   StartIcon="@Icons.Material.Filled.DeleteForever"
                   OnClick="ConfirmReset">
            ล้างข้อมูลและเริ่มใหม่
        </MudButton>
    </DialogActions>
</MudDialog>

<style>
    /* ==========================================================================
       Pinyin Island Landing Page Styles - Kid-Friendly 3D Aesthetic
       ========================================================================== */
    .landing-container {
        position: relative;
        height: 100vh;
        max-height: 100vh;
        width: 100%;
        box-sizing: border-box;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        align-items: center;
        padding: 0.75rem 1.25rem;
        background-image: linear-gradient(180deg, rgba(255, 255, 255, 0.25) 0%, rgba(224, 247, 250, 0.4) 100%),
                          url('assets/images/bg-beach.jpeg');
        background-size: cover;
        background-position: center bottom;
        background-attachment: fixed;
        overflow: hidden;
        font-family: 'Fredoka', 'Prompt', sans-serif;
    }

    /* Top Controls Bar */
    .top-controls {
        width: 100%;
        max-width: 1200px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        z-index: 10;
        padding: 0.2rem 0.5rem;
    }

    .grade-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        background: rgba(255, 255, 255, 0.92);
        backdrop-filter: blur(8px);
        padding: 0.4rem 1rem;
        border-radius: 999px;
        box-shadow: 0 6px 16px rgba(0, 0, 0, 0.08);
        border: 2px solid #FFF176;
    }

    .badge-emoji {
        font-size: 1.3rem;
    }

    .badge-text {
        font-weight: 700;
        font-size: 1rem;
        color: #E65100;
        letter-spacing: 0.5px;
    }

    .controls-right {
        display: flex;
        gap: 0.75rem;
        align-items: center;
    }

    /* 3D Tactile Action Buttons (Small round) */
    .tactile-icon-btn {
        width: 48px;
        height: 48px;
        border-radius: 16px;
        border: none;
        cursor: pointer;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: transform 0.1s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.1s ease;
        outline: none;
        user-select: none;
        -webkit-tap-highlight-color: transparent;
    }

    .btn-blue {
        background: linear-gradient(145deg, #42A5F5 0%, #1E88E5 100%);
        color: #FFFFFF;
        box-shadow: 0 5px 0 #1565C0, 0 8px 16px rgba(30, 136, 229, 0.4);
        border: 2px solid #90CAF9;
    }

    .btn-blue.muted {
        background: linear-gradient(145deg, #B0BEC5 0%, #78909C 100%);
        box-shadow: 0 5px 0 #546E7A, 0 8px 16px rgba(84, 110, 122, 0.3);
        border-color: #CFD8DC;
    }

    .btn-red {
        background: linear-gradient(145deg, #FF6B6B 0%, #E53935 100%);
        color: #FFFFFF;
        box-shadow: 0 5px 0 #C62828, 0 8px 16px rgba(229, 57, 53, 0.4);
        border: 2px solid #FFCDD2;
    }

    .tactile-icon-btn:hover {
        transform: translateY(-2px);
    }

    .tactile-icon-btn:active {
        transform: translateY(4px);
        box-shadow: 0 2px 0 currentColor, 0 4px 8px rgba(0, 0, 0, 0.2) !important;
    }

    .tactile-btn-icon {
        font-size: 1.6rem !important;
    }

    /* Main Hero Area - Spacious 2-Column Desktop Grid */
    .hero-content {
        display: grid;
        grid-template-columns: 1fr 1fr;
        grid-template-rows: auto auto;
        grid-template-areas: 
            "mascot logo"
            "mascot cta";
        align-items: center;
        justify-items: center;
        column-gap: 48px;
        row-gap: 18px;
        width: 100%;
        max-width: 820px;
        margin: auto;
        padding: 0.5rem 0;
        z-index: 5;
    }

    /* Logo Styling */
    .logo-wrapper {
        grid-area: logo;
        margin-bottom: 0;
        display: inline-flex;
        justify-content: center;
        align-items: center;
        width: fit-content;
        max-width: 460px;
        border-radius: 30px;
        background: #FFFFFF;
        border: 4.5px solid #FFF59D;
        box-shadow: 0 10px 24px rgba(0, 0, 0, 0.12), 0 0 0 2px rgba(255, 238, 88, 0.35);
        padding: 6px 22px;
        overflow: hidden;
        position: relative;
    }

    .pinyin-logo {
        height: 160px;
        width: auto;
        max-width: 100%;
        object-fit: contain;
        transform: scale(1.1);
        display: block;
    }

    /* Mascot Presentation */
    .mascot-stage {
        grid-area: mascot;
        position: relative;
        display: flex;
        flex-direction: column;
        align-items: center;
        margin: 0;
    }

    /* Floating Chest in the Right Sea Area */
    .chest-floating-sea {
        position: absolute;
        right: 4%;
        top: auto;
        bottom: 16%;
        z-index: 15;
        pointer-events: auto;
        cursor: pointer;
    }

    .chest-inner-anim {
        display: flex;
        flex-direction: column;
        align-items: center;
        position: relative;
        animation: seaWaveFloat 4.5s ease-in-out infinite;
        will-change: transform;
    }

    .chest-inner-anim:hover {
        animation-play-state: paused;
        transform: scale(1.12) rotate(-4deg);
    }

    .chest-sea-img {
        width: 130px;
        height: auto;
        border-radius: 22px;
        box-shadow: 0 12px 24px rgba(0, 137, 123, 0.35), 0 0 22px rgba(255, 215, 0, 0.6);
        border: 3px solid #FFE082;
        background: rgba(255, 255, 255, 0.9);
        display: block;
    }

    .chest-sparkle {
        position: absolute;
        top: -12px;
        right: -8px;
        font-size: 1.6rem;
        animation: sparkleAnim 1.8s infinite ease-in-out;
        pointer-events: none;
    }

    .mascot-frame {
        position: relative;
        width: 230px;
        height: 230px;
        border-radius: 50%;
        overflow: hidden;
        border: 5px solid #FFFFFF;
        box-shadow: 0 12px 28px rgba(0, 150, 136, 0.28), 0 0 0 3px #80DEEA;
        background: radial-gradient(circle, #FFF9C4 0%, #B2EBF2 100%);
    }

    .mascot-video {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
    }

    .mascot-fallback {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .mascot-shadow {
        position: absolute;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
        width: 150px;
        height: 14px;
        background: radial-gradient(ellipse at center, rgba(0, 0, 0, 0.25) 0%, rgba(0, 0, 0, 0) 70%);
        border-radius: 50%;
    }

    .island-speech-bubble {
        margin-top: 0.75rem;
        background: #FFFFFF;
        padding: 0.45rem 1.2rem;
        border-radius: 20px;
        font-weight: 700;
        font-size: 0.95rem;
        color: #00796B;
        box-shadow: 0 6px 16px rgba(0, 0, 0, 0.08);
        border: 2px solid #80CBC4;
        animation: pulseSubtle 3s infinite ease-in-out;
        max-width: 320px;
        text-align: center;
    }

    /* Giant 3D Play Now Button (Tactile Claymorphism) */
    .cta-container {
        grid-area: cta;
        width: 100%;
        max-width: 330px;
        margin-top: 0;
        display: flex;
        justify-content: center;
    }

    .play-now-tactile-btn {
        position: relative;
        width: 100%;
        height: 76px;
        border: none;
        border-radius: 38px;
        cursor: pointer;
        outline: none;
        user-select: none;
        -webkit-tap-highlight-color: transparent;
        background: linear-gradient(180deg, #66BB6A 0%, #43A047 60%, #2E7D32 100%);
        box-shadow: 0 8px 0 #1B5E20, 0 14px 26px rgba(27, 94, 32, 0.45);
        border: 3px solid #A5D6A7;
        transition: transform 0.12s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.12s ease;
        animation: heroPulse 2.5s infinite ease-in-out;
        padding: 0 1.25rem;
    }

    .play-now-tactile-btn:hover {
        transform: translateY(-3px) scale(1.02);
        box-shadow: 0 11px 0 #1B5E20, 0 18px 30px rgba(27, 94, 32, 0.5);
    }

    .play-now-tactile-btn:active {
        transform: translateY(6px) scale(0.98);
        box-shadow: 0 2px 0 #1B5E20, 0 4px 10px rgba(27, 94, 32, 0.3) !important;
        animation: none;
    }

    .play-btn-content {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 1rem;
        height: 100%;
    }

    .play-icon-circle {
        width: 46px;
        height: 46px;
        border-radius: 50%;
        background: #FFFFFF;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
    }

    .play-arrow-svg {
        font-size: 2.2rem !important;
        color: #2E7D32 !important;
        margin-left: 3px;
    }

    .play-btn-labels {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        text-align: left;
    }

    .play-btn-primary {
        font-size: 1.65rem;
        font-weight: 800;
        color: #FFFFFF;
        text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);
        line-height: 1.15;
        letter-spacing: 0.5px;
    }

    .play-btn-sub {
        font-size: 0.78rem;
        font-weight: 700;
        color: #C8E6C9;
        letter-spacing: 1.2px;
    }

    /* Landing Footer */
    .landing-footer {
        z-index: 10;
        margin-top: 0.4rem;
        background: rgba(255, 255, 255, 0.88);
        backdrop-filter: blur(6px);
        padding: 0.35rem 1.1rem;
        border-radius: 999px;
        font-size: 0.88rem;
        font-weight: 600;
        color: #37474F;
        border: 1px solid rgba(255, 255, 255, 0.6);
        text-align: center;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.6rem;
        box-shadow: 0 4px 14px rgba(0, 0, 0, 0.06);
    }

    .version-badge {
        font-size: 0.75rem;
        font-weight: 700;
        color: #00796B;
        background: #E0F2F1;
        border: 1px solid #80CBC4;
        padding: 2px 8px;
        border-radius: 999px;
        letter-spacing: 0.5px;
        user-select: all;
    }

    /* Dialog Styles */
    .dialog-title-custom {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        font-weight: 700;
        font-size: 1.2rem;
        color: #C62828;
    }

    .dialog-title-icon {
        font-size: 1.5rem;
    }

    .dialog-body-custom {
        font-size: 1rem;
        color: #455A64;
    }

    .dialog-warning-text {
        margin-bottom: 1rem;
        line-height: 1.5;
    }

    .parent-math-check {
        background: #FFF3E0;
        border-radius: 12px;
        padding: 1rem;
        border: 1px dashed #FFA726;
    }

    .math-question {
        margin: 0;
        color: #E65100;
        font-size: 1.05rem;
    }

    /* ==========================================================================
       Animations
       ========================================================================== */
    @@keyframes float {
        0%, 100% {
            transform: translateY(0px) rotate(0deg);
        }
        50% {
            transform: translateY(-8px) rotate(-1deg);
        }
    }

    @@keyframes bounceMascot {
        0%, 100% {
            transform: translateY(0) scale(1);
        }
        50% {
            transform: translateY(-10px) scale(1.02);
        }
    }

    @@keyframes heroPulse {
        0%, 100% {
            transform: scale(1);
        }
        50% {
            transform: scale(1.03);
            box-shadow: 0 12px 0 #1B5E20, 0 20px 35px rgba(46, 125, 50, 0.6);
        }
    }

    @@keyframes pulseSubtle {
        0%, 100% {
            transform: scale(1);
        }
        50% {
            transform: scale(1.04);
        }
    }

    @@keyframes seaWaveFloat {
        0% {
            transform: translate3d(0, 0, 0) rotate(0deg);
        }
        20% {
            transform: translate3d(8px, -18px, 0) rotate(4deg);
        }
        40% {
            transform: translate3d(16px, -6px, 0) rotate(1deg);
        }
        60% {
            transform: translate3d(8px, -24px, 0) rotate(-4deg);
        }
        80% {
            transform: translate3d(-4px, -10px, 0) rotate(-2deg);
        }
        100% {
            transform: translate3d(0, 0, 0) rotate(0deg);
        }
    }

    @@keyframes sparkleAnim {
        0%, 100% {
            transform: scale(0.8) rotate(0deg);
            opacity: 0.7;
        }
        50% {
            transform: scale(1.3) rotate(20deg);
            opacity: 1;
        }
    }

    .floating-anim {
        animation: float 4s ease-in-out infinite;
    }

    .bounce-anim {
        animation: bounceMascot 3.2s ease-in-out infinite;
    }

    .sea-float-anim {
        animation: seaWaveFloat 5.5s ease-in-out infinite;
    }

    /* Media query adjustments for tablets & small screens */
    @@media (max-width: 900px) {
        .chest-floating-sea {
            right: 2%;
            top: auto;
            bottom: calc(75px + env(safe-area-inset-bottom, 0px));
        }
        .chest-sea-img {
            width: 90px;
        }
    }

    @@media (max-height: 540px) and (orientation: landscape) {
        .landing-container {
            height: 100vh;
            min-height: 100vh;
            padding: calc(0.35rem + env(safe-area-inset-top, 0px)) calc(1.5rem + env(safe-area-inset-right, 0px)) calc(0.35rem + env(safe-area-inset-bottom, 0px)) calc(1.5rem + env(safe-area-inset-left, 0px));
            justify-content: space-between;
            overflow: hidden;
        }

        .top-controls {
            padding: 0 0.5rem;
        }

        .grade-badge {
            padding: 0.25rem 0.75rem;
        }

        .badge-text {
            font-size: 0.85rem;
        }

        .badge-emoji {
            font-size: 1.1rem;
        }

        .tactile-icon-btn {
            width: 40px;
            height: 40px;
            border-radius: 12px;
        }

        .tactile-btn-icon {
            font-size: 1.3rem !important;
        }

        .hero-content {
            display: grid;
            grid-template-columns: auto 1fr;
            grid-template-rows: auto auto;
            grid-template-areas: 
                "mascot logo"
                "mascot cta";
            align-items: center;
            justify-content: center;
            gap: 8px 24px;
            max-width: 720px;
            margin: auto;
            padding: 0;
            width: 100%;
        }

        .mascot-stage {
            grid-area: mascot;
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .mascot-frame {
            width: 135px;
            height: 135px;
            border-width: 4px;
        }

        .island-speech-bubble {
            margin-top: 6px;
            font-size: 0.78rem;
            padding: 0.25rem 0.75rem;
            max-width: 220px;
            text-align: center;
            border-radius: 14px;
        }

        .logo-wrapper {
            grid-area: logo;
            margin-bottom: 0;
            width: fit-content;
            max-width: 300px;
            padding: 3px 14px;
            border-radius: 20px;
            border-width: 3px;
        }

        .pinyin-logo {
            height: 86px;
            transform: scale(1.1);
        }

        .cta-container {
            grid-area: cta;
            margin-top: 0;
            max-width: 280px;
            width: 100%;
            justify-self: center;
        }

        .play-now-tactile-btn {
            height: 60px;
            padding: 0 1.25rem;
            border-radius: 30px;
        }

        .play-icon-circle {
            width: 40px;
            height: 40px;
        }

        .play-arrow-svg {
            font-size: 1.8rem !important;
        }

        .play-btn-primary {
            font-size: 1.3rem;
        }

        .play-btn-sub {
            font-size: 0.7rem;
            letter-spacing: 1px;
        }

        .chest-floating-sea {
            display: block;
            right: calc(20px + env(safe-area-inset-right, 0px));
            top: auto;
            bottom: calc(44px + env(safe-area-inset-bottom, 0px));
            transform: none;
            z-index: 15;
        }

        .chest-sea-img {
            width: 82px;
            border-radius: 16px;
            border-width: 2.5px;
            box-shadow: 0 8px 16px rgba(0, 137, 123, 0.3), 0 0 16px rgba(255, 215, 0, 0.5);
        }

        .chest-sparkle {
            font-size: 1.1rem;
            top: -10px;
            right: -6px;
        }

        .landing-footer {
            margin-top: 0;
            font-size: 0.72rem;
            padding: 0.2rem 0.75rem;
        }
    }

    @@media (max-width: 600px) and (orientation: portrait) {
        .landing-container {
            padding: calc(0.9rem + env(safe-area-inset-top, 0px)) calc(0.9rem + env(safe-area-inset-right, 0px)) calc(0.9rem + env(safe-area-inset-bottom, 0px)) calc(0.9rem + env(safe-area-inset-left, 0px));
        }

        .hero-content {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
            max-width: 100%;
        }

        .chest-floating-sea {
            display: none;
        }

        .mascot-stage {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 12px;
            margin: 0;
        }

        .mascot-frame {
            width: 190px;
            height: 190px;
        }

        .island-speech-bubble {
            margin-top: 0;
            font-size: 0.95rem;
            padding: 0.45rem 1.1rem;
        }

        .cta-container {
            margin-top: 2px;
            width: 100%;
            max-width: 320px;
        }

        .play-now-tactile-btn {
            height: 72px;
            padding: 0 1rem;
        }

        .play-btn-primary {
            font-size: 1.45rem;
        }

        .logo-wrapper {
            width: fit-content;
            max-width: 90vw;
            padding: 4px 16px;
            border-radius: 24px;
        }

        .pinyin-logo {
            height: 118px;
            transform: scale(1.1);
        }

        .grade-badge {
            padding: 0.35rem 0.8rem;
        }

        .badge-text {
            font-size: 0.9rem;
        }

        .tactile-icon-btn {
            width: 46px;
            height: 46px;
        }

        .landing-footer {
            font-size: 0.78rem;
            padding: 0.3rem 0.8rem;
        }
    }
</style>

@code {
    private bool _isMuted = false;
    private bool _resetDialogVisible = false;
    private string _mathAnswer = string.Empty;
    private string _appVersion = "v1.0.0";

    private readonly DialogOptions _dialogOptions = new()
    {
        MaxWidth = MaxWidth.Small,
        FullWidth = true,
        CloseButton = true,
        BackdropClick = false
    };

    protected override async Task OnInitializedAsync()
    {
        try
        {
            var res = await Http.GetFromJsonAsync<VersionResponse>("version.json");
            if (!string.IsNullOrWhiteSpace(res?.Version))
            {
                _appVersion = res.Version.StartsWith("v", StringComparison.OrdinalIgnoreCase)
                    ? res.Version
                    : $"v{res.Version}";
            }
        }
        catch
        {
            // Fallback to default version if file not present
        }
    }

    private class VersionResponse
    {
        public string? Version { get; set; }
    }

    private void StartGame()
    {
        Nav.NavigateTo("/map");
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            try
            {
                // Ensure map BGM stops on home page
                await JS.InvokeVoidAsync("gameAudio.pauseBgm");

                // Ensure video autoplays smoothly across all browsers
                await JS.InvokeVoidAsync("gameAudio.playVideo", "mascotVideo");
                await JS.InvokeVoidAsync("gameAudio.setVideoMuted", "mascotVideo", _isMuted);
            }
            catch
            {
                // Silently ignore if JS runtime not ready
            }
        }
    }

    private async Task ToggleSound()
    {
        _isMuted = !_isMuted;
        try
        {
            await JS.InvokeVoidAsync("gameAudio.setVideoMuted", "mascotVideo", _isMuted);
        }
        catch
        {
            // Ignored if runtime not available
        }

        var message = _isMuted ? "ปิดเสียงเรียบร้อย 🔇" : "เปิดเสียงแล้ว! 🎵";
        Snackbar.Add(message, Severity.Info, config =>
        {
            config.VisibleStateDuration = 1800;
            config.ShowCloseIcon = false;
        });
    }

    private void OpenResetConfirmDialog()
    {
        _mathAnswer = string.Empty;
        _resetDialogVisible = true;
    }

    private void CloseResetDialog()
    {
        _resetDialogVisible = false;
        _mathAnswer = string.Empty;
    }

    private async Task ConfirmReset()
    {
        // Simple security gate for parents / teachers (3 + 4 = 7)
        if (_mathAnswer.Trim() == "7")
        {
            await ProgressService.ResetProgressAsync();
            _resetDialogVisible = false;
            Snackbar.Add("รีเซ็ตความก้าวหน้าสำเร็จ เริ่มต้นการผจญภัยใหม่! ✨ (0 ดาว)", Severity.Success);
        }
        else
        {
            Snackbar.Add("คำตอบไม่ถูกต้อง กรุณาตรวจสอบคำตอบ (3 + 4 = 7)", Severity.Warning);
        }
    }
}

